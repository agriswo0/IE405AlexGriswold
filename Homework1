#include <iostream>

int main() {
    int year;
    std::cout<<"Enter the year to be tested: ";
    std::cin>>year;
    
    double val = year%4;
    
    if(val!=0){
        std::cout<<"Nope, it's not a leap year.";
    }
    else{
        std::cout<<"Yes, it's a leap year.";
    }
}
